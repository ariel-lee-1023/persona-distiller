{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/ariel-lee-1023/persona-distiller/main/references/schemas/coverage-map.schema.json",
  "title": "coverage_map.json",
  "description": "Stage 1 output. The backbone of the honest coverage report and of several auto-weighting defaults. Computed after segmentation; consumed by Stage 2 (pass weighting), Stage 3 (probe weighting), and the user-facing coverage report.",
  "type": "object",
  "required": ["total_tokens", "n_clusters", "domains", "dialogue_ratio", "decision_density"],
  "additionalProperties": false,
  "properties": {
    "total_tokens": {
      "type": "integer",
      "minimum": 0,
      "description": "Total tokens across all clusters. Very low values mean planning for a reduced-scope core up front."
    },
    "n_clusters": {
      "type": "integer",
      "minimum": 1,
      "description": "Must equal the length of clusters[] in clusters/manifest.json."
    },
    "domains": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 },
      "description": "Subject areas the corpus actually covers. A single narrow slice means a reduced-scope core."
    },
    "dialogue_ratio": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "(dialogue + decision_record tokens) / total_tokens. High → raise the interactional pass and probe weight; low → lean on projectible-regularity extraction."
    },
    "decision_density": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "decision_record tokens / total_tokens."
    },
    "temporal_spread": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "earliest": { "type": "string", "description": "Year of the earliest dated cluster, e.g. '2006'." },
        "latest": { "type": "string", "description": "Year of the latest dated cluster, e.g. '2024'." },
        "gaps": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Unattested spans, e.g. '2011-2014'. These become explicit caveats in the coverage report."
        }
      }
    },
    "thin_domains": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Domains present but under-attested. Must be surfaced in the coverage report — never inside the persona."
    },
    "sources": {
      "type": "array",
      "items": { "$ref": "#/$defs/source" },
      "description": "One acquisition record per source the corpus was built from. Omit for a purely local corpus; required in practice for anything remote, since it is the only place the retrieval is recorded."
    },
    "firsthand_ratio": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Firsthand tokens / total_tokens, from the attribution labels in clusters/manifest.json. A low value is a coverage-report caveat, not a blocker — but the user needs to see it, because it is the number that says how much of this persona came from the person rather than from people writing about them."
    },
    "notes": {
      "type": "string",
      "description": "Free-text observations about corpus shape."
    }
  },
  "$defs": {
    "source": {
      "type": "object",
      "required": ["type", "location"],
      "additionalProperties": false,
      "properties": {
        "type": {
          "type": "string",
          "enum": ["local", "git", "file_url", "site"],
          "description": "How it was acquired. See acquisition.md for what each one means and how to fetch it."
        },
        "location": {
          "type": "string",
          "minLength": 1,
          "description": "Path for local sources; URL for remote ones. For a site, the path prefix that was crawled — never the bare domain."
        },
        "retrieved": {
          "type": "string",
          "format": "date",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "ISO 8601 retrieval date. Omit only for local sources."
        },
        "revision": {
          "type": "string",
          "minLength": 1,
          "description": "Commit SHA for git sources."
        },
        "licence": {
          "type": "string",
          "description": "The licence the source publishes under, when it states one. A knowledge base's own licence is binding even when its subject is your subject."
        }
      }
    }
  },
  "examples": [
    {
      "total_tokens": 128000,
      "n_clusters": 14,
      "domains": ["political philosophy", "religion", "economics"],
      "dialogue_ratio": 0.35,
      "decision_density": 0.12,
      "temporal_spread": { "earliest": "2006", "latest": "2024", "gaps": ["2011-2014"] },
      "thin_domains": ["foreign policy"],
      "sources": [
        { "type": "local", "location": "uploads/book.pdf" },
        {
          "type": "git",
          "location": "https://github.com/example/notes.git",
          "retrieved": "2026-07-27",
          "revision": "9f2c1ab7d4e5f60123456789abcdef0123456789",
          "licence": "CC BY-SA 4.0"
        },
        { "type": "site", "location": "https://example.org/kb/notes/", "retrieved": "2026-07-27" }
      ],
      "firsthand_ratio": 0.62,
      "notes": "Heavy on monologic prose; little live dialogue. Roughly a third is secondhand reading notes — corroborating only."
    }
  ]
}
